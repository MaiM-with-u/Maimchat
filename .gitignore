# =========================================
# Created by the Android Studio Git plugin
# =========================================

# Build files
*.apk
!build/published-apks/**/*.apk
*.aab
*.ap_
*.dex
*.class
*.min.jar

# Folders generated by the build system
build/
!build/published-apks/
!build/published-apks/**
.gradle/
captures/

# Local configuration files (important to ignore)
# This file contains the path to the SDK. It should be created by each developer locally.
local.properties

# IDE files (Android Studio & IntelliJ)
.idea/caches/
.idea/libraries/
.idea/modules.xml
.idea/workspace.xml
.idea/navEditor.xml
.idea/assetWizardSettings.xml
*.iml
*.ipr
*.iws
.DS_Store

# Keystore files (NEVER commit these)
*.jks
*.keystore

# Explicit keystore directory (added)
keystore/
**/keystore/**

# Live2D SDK (download separately)
app/CubismSdkForJava-*/
app/libs/Live2DCubismCore*.aar
app/src/main/assets/**
!app/src/main/assets/README.md

# Possible signing config/property files
signing.properties
keystore.properties
key.properties
*.keystore.properties

# Google Services file (if it contains sensitive info, consider ignoring)
# google-services.json

# Log files
*.log
*.hprof
*.trace

# Android/NDK native intermediate & external builds
.externalNativeBuild/
obj/

# Fastlane (if later added for Play publishing)
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/**
fastlane/test_output/**
fastlane/README.md

# Generated Google / Firebase config backups (keep real one out if sensitive)
google-services.json.bak

# Gradle build cache (already have .gradle/, add additional safety for nested)
**/.gradle/**

# Idea misc (reinforce in case of submodules)
**/.idea/**

# Kotlin incremental compilation caches (in case inside custom dirs)
**/kotlin/**/incremental/**

# Lint / analysis extra outputs
lint/outputs/

# Temporary scripts or local helper files
*.local.sh
*.secret.sh
keygen.sh

# Crash / ANR traces you might drop here manually
anr-traces.txt
crash.log

# Misc system files
Thumbs.db
ehthumbs.db
*.swp
*.swo

# Build artifacts (reinforce – already covered but explicit clarity)
app/release/
*.aab

# Live2D / model cache (if you create one later)
live2d_cache/


# C/C++ native code builds
.cxx/
*/build/
*/.cxx/

# Lint reports
lint/reports/
lint-results-*.xml
lint-results-*.html